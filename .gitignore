# ── TUYỆT ĐỐI KHÔNG ĐẨY LÊN ─────────────────────────────
# Data pack của khoá là tài liệu nội bộ. Repo này PUBLIC.
data/
*.csv
vlearn-pack/
discord-pack/
studio-pack/

# Khoá API — lộ là mất tài khoản
.env
.env.*
!.env.example
*.key
*.pem
secrets.json
**/sources.local.json

.agents
.codex

# Rác hệ thống
.DS_Store
__pycache__/
node_modules/

# Log ghi vết lời gọi LLM (runtime, có thể dài + không cần commit)
codebase/logs/

# venv cục bộ (Python sạch cho openai SDK)
.venv/
